import React from 'react';
import { Text, View, Image, StyleSheet } from 'react-native';

const HeaderTitle = ({
    params,
}) => (
    <Image source={require("../../assets/image/header-logo.png")} />
);


export default HeaderTitle;
